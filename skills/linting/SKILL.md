# Linting Skill

Runs project-specific linters and formats the output for agent consumption.

## Files

- `run.sh` — Entry point. Detects the project type and runs the appropriate linter.
- `parse-output.sh` — Parses linter output into a structured format.
